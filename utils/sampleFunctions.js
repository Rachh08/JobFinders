// function validatePassword(password) {
//     // Password must have exactly 8 characters
//     const hasExactLength = password.length === 8;

//     // Password must have a mix of special characters, uppercase letters, and lowercase letters
//     const hasMixCharacters = /^(?=.*[a-z])(?=.*[A-Z])(?=.*[!@#$%^&*])/.test(password);

//     // Combine conditions to determine overall password strength
//     const isStrongPassword = hasExactLength && hasMixCharacters;

//     return isStrongPassword;
// }

// module.exports = {
//     validatePassword
// };
